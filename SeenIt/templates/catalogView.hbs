{{> header}}
<div class="content">
    {{> navigation}}
    <section id="viewCatalog">
        <div class="posts">
            {{#each posts}}
                {{> post}}
            {{/each}}
        </div>
    </section>
</div>
{{> footer}}